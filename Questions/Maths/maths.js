//Program to find Area of a circle

function areaCircle(r) {
    let equation = Math.round(22 / 7 * r * r);
    return equation;
}
let myequation = areaCircle( 12);
console.log("Area of given circle is "+ myequation);

//---------------------------------------------------------------------------------------------------------------------------
//Program to find Area of a Triangle.

function areaTriangle( b , h ) {
    console.log(Math.round(0.5 * b * h ) + " is area of given triangle");
}
areaTriangle( 80 , 6 )
//---------------------------------------------------------------------------------------------------------------------------
// program to find all angles of a triangle.

function lcm( a , b ) {
}


lcm( 4,8);


